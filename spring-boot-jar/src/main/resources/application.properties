# Scaffold resource file. Kept so the project mirrors a typical Vaadin
# layout; it is declared as an input of vaadinBuildFrontend, so the
# build-cache scenarios deliberately do not edit it (see
# messages.properties for the resource edited in scenario C).
spring.application.name=spring-boot-jar
server.port=8080
