# Scaffold resource file. The build-cache test suite appends to this file
# in scenario C to verify that a resource edit does not invalidate the
# vaadinBuildFrontend cache.
demo.name=plain-jar
demo.environment=test
