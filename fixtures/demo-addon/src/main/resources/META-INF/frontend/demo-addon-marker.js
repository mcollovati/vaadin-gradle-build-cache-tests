// Frontend resource shipped inside the demo-addon fixture jar. Its
// presence in the produced bundle (via stats.json bundleImports) proves
// that a frontend dependency carried by a jar is part of
// vaadinBuildFrontend's cache key.
export const demoAddonMarker = 'demo-addon-marker';
